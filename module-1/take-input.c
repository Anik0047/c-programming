#include <stdio.h>
int main()
{

    // int x, y;
    // scanf("%d %d", &x, &y);
    // printf("%d %d", x, y);

    // int x;
    // float y;
    // char z;
    // scanf("%d %f %c", &x, &y, &z);
    // printf("%d %0.2f %c %c", x, y, z);

    int x, y;
    scanf("%d%% %d%%", &x, &y);
    printf("%d%% %d%%", x, y);

    return 0;
}